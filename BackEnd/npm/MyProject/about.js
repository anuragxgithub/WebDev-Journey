/*
We create our own package.json for our project.
Use the command "npm init"    to initialize it.

So if we now install any package in this directory so our own packge.json 
got updated no new package.json will be created after installation.

*/